# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Jupyter
.ipynb_checkpoints/

# Env / secrets
.env
*.pem
*.key
# All crawl/verify intermediates and logs (jsonl, tmp, logs)
scrape_data/

*.lock

# Everything under data/ except the metadata summaries.
# `data/*` (not `data/`) so the negation below can re-include a file.
data/*
!data/meta_data.json
!data/mycoast_meta_data.json
